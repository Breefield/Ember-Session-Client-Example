Ember-Session-Client-Example
============================

Trying to determine best way to create auth session between ember client and rails api which do not share the same domain (not using ember rails).
Looking at you ember-auth.
